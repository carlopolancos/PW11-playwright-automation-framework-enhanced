-run Jenkins (java -Dhudson.model.DirectoryBrowserSupport.CSP="" -jar jenkins.war)
-install plugin cucumber reports

-free form project type + name
-from directory, copy folder path

-this project is parameterized (/)
--Choice Parameter
---TAG (regression, contact-us, login, smoke)
---THREAD_COUNT (1, 2, 3, 4, 5)
---BROWSER (chromium, firefox)

-Build Steps
--Execute Windows batch command
---mvn clean compile test -Dcucumber.filter.tags="(@%TAG% and (not @ignore))" -Ddataproviderthreadcount=%THREAD_COUNT% -DBROWSER=%BROWSER%

-add post build setup
**/target/*.json

